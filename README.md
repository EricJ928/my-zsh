# my-zsh

## Steps

1. Change shell to zsh
```zsh
chsh -s /bin/zsh
echo $SHELL
```
2. Insert all dot files to respective original files (at start)
3. For VS code shell integration:
4. Open Settings `cmd` + `,`, search for "Terminal › Integrated › Default Profile: Osx". Change it to "zsh"
